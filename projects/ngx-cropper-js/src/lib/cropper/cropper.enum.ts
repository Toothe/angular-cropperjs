/**
 * ViewMode setting options
 */
export enum CropperViewMode {
    NoRestrictions,
    InsideCanvas,
    MinimumFit,
    MinimumFill,
}

/**
 * DragMode setting options
 */
export enum CropperDragMode {
    Crop = 'crop',
    Move = 'move',
    None = 'none',
}
